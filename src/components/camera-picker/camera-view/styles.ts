import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  noDevices: {alignItems: 'center', justifyContent: 'center'},
  container: {flex: 1},
  takePicBtn: {
    position: 'absolute',
    width: 100,
    height: 50,
    bottom: 20,
    alignSelf: 'center',
    alignItems: 'center',
  },
});
