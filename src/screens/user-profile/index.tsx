import {SafeAreaView, Text, View} from 'react-native';

const UserProfile = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>User Profile</Text>
      </View>
    </SafeAreaView>
  );
};

export default UserProfile;
